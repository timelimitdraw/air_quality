<!-- Validation failure messages -->
<% if @zip.errors.any? %>
  <% @zip.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Zip</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_zip" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for user_id -->
      <input type="hidden" name="user_id" value="<%= current_user.id %>">

      <!-- Label and input for zip_code -->
      <div class="form-group">
        <label for="zip_code" class="control-label">
          Zip code
        </label>

        <input type="text" id="zip_code" name="zip_code" placeholder="Zip code" class="form-control" value="<%= @zip.zip_code %>">
      </div>

      <button class="btn btn-success">
        Create Zip
      </button>
      or
      <a href="/zips">Cancel</a>
    </form>
  </div>
</div>
